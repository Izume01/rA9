{
  "name": "Ra9",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "dev:cli": "bun run --cwd apps/cli dev",
    "build": "turbo run build",
    "clean": "turbo run clean"
  },
  "packageManager": "bun@1.3.6",
  "devDependencies": {
    "turbo": "^2.9.15"
  }
}
